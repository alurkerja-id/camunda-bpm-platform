<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <artifactId>camunda-ejb-client</artifactId>
  <groupId>alurkerja.camunda.bpm.javaee</groupId>
  <name>Camunda Platform - Client</name>
  <packaging>jar</packaging>

  <properties>
    <!-- Alurkerja fork: Java EE integration only works inside an application server container.
         This fork ships libraries and deploys nothing onto one, so it is out of the SonarQube
         analysis - same reasoning as distro/wildfly. -->
    <sonar.skip>true</sonar.skip>
  </properties>

  <description>
    Note: 7.24.0 is the last community edition release published on Maven Central. This library will not receive any new versions or releases. However, if you are interested in extended maintenance you could look into our enterprise edition. Get in touch with us: https://camunda.com/contact/
  </description>

  <parent>
    <groupId>alurkerja.camunda.bpm</groupId>
    <artifactId>camunda-parent</artifactId>
    <relativePath>../../parent</relativePath>
    <version>${revision}</version>
  </parent>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>alurkerja.camunda.bpm</groupId>
        <artifactId>camunda-core-internal-dependencies</artifactId>
        <version>${project.version}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.jboss.spec</groupId>
      <artifactId>jboss-javaee-web-6.0</artifactId>
      <type>pom</type>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>alurkerja.camunda.bpm</groupId>
      <artifactId>camunda-engine</artifactId>
      <scope>provided</scope>
      <exclusions>
        <exclusion>
          <artifactId>livetribe-jsr223</artifactId>
          <groupId>org.livetribe</groupId>
        </exclusion>
      </exclusions>

    </dependency>

  </dependencies>

</project>
